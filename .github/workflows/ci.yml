name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  tests:
    runs-on: self-hosted
    steps:
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24.x'

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run tests
        run: TEST_WORKERS=5 make tests-parallel