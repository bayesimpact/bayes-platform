name: CI

on:
  push:
    branches:
      - 'chore/changelog-v*'
    paths:
      - CHANGELOG.md

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Changelog-only PR — skipping tests"