# PyTorch CUDA 12.1 wheels. Kept in a separate file because this index only
# hosts torch packages — installing everything else from it would fail.
--index-url https://download.pytorch.org/whl/cu121

torch==2.5.1
torchvision==0.20.1
