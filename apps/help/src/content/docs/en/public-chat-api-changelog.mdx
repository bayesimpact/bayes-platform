---
title: Public chat API changelog
highlight: changelog
description: Every version of the public chat API, with the date, the changes and the migration steps when a change is breaking.
category: developers
order: 3
updated: 2026-09-09
apiVersion: "1.0"
---

This page lists every version of the public chat API. The current version is **1.0**.
Minor versions add things and keep existing clients working. Major versions can break
clients and come with a deprecation period. See the
[versioning and deprecation policy](/en/public-chat-api-versioning).

{/*
  Entry template. Keep the heading format: the CI check reads "## <major.minor> (<date>)".

  ## 1.1 (YYYY-MM-DD)

  ### Added
  - ...

  ### Changed
  - ...

  ### Deprecated
  - ...

  ### Removed
  - ...

  ### Migration
  - ... (mandatory for a major version)
*/}

## 1.0 (2026-09-09)

### Added

- First versioned release of the public chat API, under `/public/v1/agents/{embedToken}/`.
- Five routes: agent configuration, create a session, read a session, read MCP App card
  HTML, send a message and stream the reply.
- The [reference page](/en/public-chat-api) documents the routes, the server-sent events,
  the error codes, the CORS behavior and the limits.

### Changed

- The unprefixed paths `/public/agents/{embedToken}/...` become a legacy alias of version
  1, frozen at their pre-versioning shape: no MCP App card HTML route, no `bannerText`. They
  keep answering for as long as version 1 is served.

### Migration

- None. Existing clients keep working without change. Move to the `/public/v1/` paths
  to get `bannerText` and the MCP App card HTML route.
